language: crystal
crystal:
  - latest
  - nightly

script:
  - crystal spec -Dtest -Dverbose
  - crystal spec -Dtest -Dverbose --release --no-debug
  - crystal tool format --check
  - bin/ameba

matrix:
  allow_failures:
    - crystal: nightly
