language: crystal
crystal:
  - latest
  - nightly

script:
  - crystal spec -Dtest -Dverbose -Dswagger
  - crystal spec -Dtest -Dverbose -Dswagger --release --no-debug
  - crystal tool format --check
  - bin/ameba

matrix:
  allow_failures:
    - crystal: nightly
